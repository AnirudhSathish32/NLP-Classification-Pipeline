version: '3.9'

services:
  sentiment-api:
    build: .
    container_name: sentiment-api
    ports:
      - "8000:8000"
    stdin_open: true
    tty: true
    command: uvicorn app:app --host 0.0.0.0 --port 8000
    volumes:
      - .:/app
